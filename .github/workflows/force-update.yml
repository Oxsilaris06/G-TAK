name: 🚨 FORCE UPDATE PRODUCTION

on:
  workflow_dispatch: # Permet le lancement manuel sans options

jobs:
  force-update:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout du code
        uses: actions/checkout@v4

      - name: 🏗 Configuration de Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: ''

      - name: 🏗 Configuration de EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_PRAXIS_SECRET }}
          packager: npm

      - name: 📦 Installation des dépendances
        run: |
          npm install --legacy-peer-deps
          npx expo install --fix -- --legacy-peer-deps

      - name: 🚀 FORCER LA MISE À JOUR (Production)
        run: |
          # On force le canal production et le message, sans demander à l'utilisateur
          eas update --branch production --message "Reset Config Clean"
