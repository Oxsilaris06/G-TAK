name: 🕵️ DIAGNOSTIC SERVEUR EAS

on:
  workflow_dispatch:

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout du code
        uses: actions/checkout@v4

      - name: 🏗 Configuration de Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 🏗 Installation EAS CLI
        run: npm install -g eas-cli

      - name: 🔍 TEST 1 - INFO PROJET (Vérifie l'ID)
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_PRAXIS_SECRET }}
        run: |
          echo "Tentative de connexion au projet..."
          # Cette commande va échouer si l'ID dans app.config.js est faux
          npx eas project:info

      - name: 🔍 TEST 2 - LISTE DES MISES À JOUR (Production)
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_PRAXIS_SECRET }}
        run: |
          echo "Récupération des mises à jour sur le canal 'production'..."
          # Affiche les 5 dernières mises à jour. 
          # On cherche une ligne avec "Runtime Version: 4.1.0"
          npx eas update:list --branch production --limit 5

      - name: 🔍 TEST 3 - LECTURE CONFIG LOCALE
        run: |
          echo "Vérification de la configuration locale :"
          cat app.config.js
