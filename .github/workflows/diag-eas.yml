name: 🕵️ DIAGNOSTIC SERVEUR EAS (FINAL)

on:
  workflow_dispatch:

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout du code
        uses: actions/checkout@v4

      - name: 🏗 Configuration de Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          # cache: 'npm' <--- LIGNE SUPPRIMÉE POUR ÉVITER L'ERREUR

      - name: 📦 Installation des dépendances
        run: npm install --legacy-peer-deps

      - name: 🏗 Installation EAS CLI
        run: npm install -g eas-cli

      - name: 🔍 TEST 1 - INFO PROJET (Vérifie l'ID)
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_PRAXIS_SECRET }}
        run: |
          echo "Tentative de connexion au projet..."
          # Si l'ID dans app.config.js est faux, ça plantera ici
          npx eas project:info

      - name: 🔍 TEST 2 - LISTE DES MISES À JOUR (Production)
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_PRAXIS_SECRET }}
        run: |
          echo "Récupération des mises à jour sur le canal 'production'..."
          # On cherche une ligne avec "Runtime Version: 4.1.0"
          npx eas update:list --branch production --limit 5 || echo "⚠️ Pas encore de mises à jour ou erreur de branche"

      - name: 🔍 TEST 3 - LECTURE CONFIG LOCALE
        run: |
          echo "Vérification de la configuration locale :"
          # On vérifie que l'URL n'a pas d'espace
          grep "url:" app.config.js
          # On vérifie la version figée
          grep "runtimeVersion:" app.config.js
