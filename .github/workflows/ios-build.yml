name: Build iOS (Unsigned IPA for SideStore)

on:
  workflow_dispatch: # Déclenchement manuel via l'interface GitHub

permissions:
  contents: write

jobs:
  build-ios:
    name: Build iOS IPA
    # UTILISATION DE MACOS 14 (Sonoma - ARM64)
    # Xcode 15.x est le défaut sur cette image, ce qui est plus stable que Xcode 16 (macos-15)
    # pour les anciennes dépendances React Native.
    runs-on: macos-14
    timeout-minutes: 60

    steps:
      - name: 🏗 Setup Repository
        uses: actions/checkout@v4

      # --- ETAPE DE VÉRIFICATION ---
      - name: 🔍 VÉRIFICATION DU FICHIER CONFIG (Preuve)
        run: |
          echo "====================================================="
          echo "CONTENU DE app.config.js SUR LE SERVEUR DE BUILD :"
          echo "====================================================="
          cat app.config.js
          echo "====================================================="
          
          if grep -q "57c6 -4432" app.config.js; then
            echo "❌ ERREUR CRITIQUE DÉTECTÉE : L'espace est PRÉSENT dans le fichier !"
            exit 1
          else
            echo "✅ SUCCÈS : Aucun espace détecté dans l'ID du fichier."
          fi

      - name: 🏗 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          # Cache désactivé pour éviter les erreurs de lockfile absent

      - name: 🏗 Setup Java (for Expo tools)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 📦 Install Dependencies
        run: |
          npm install --legacy-peer-deps
          npx expo install --fix -- --legacy-peer-deps

      # --- PATCH EXPO-DEVICE (FIX CRITIQUE) ---
      # Fix pour l'erreur "cannot find 'TARGET_OS_SIMULATOR' in scope" avec Xcode 15+
      - name: 🚑 Patch expo-device (TARGET_OS_SIMULATOR)
        run: |
          echo "Patching expo-device for Xcode 15 compatibility..."
          # On remplace la vérification dynamique par 'false' car on compile pour un DEVICE physique (Release)
          # Cela corrige l'erreur de compilation immédiate.
          # Le fichier est généralement dans node_modules/expo-device/ios/UIDevice.swift
          find node_modules/expo-device/ios -name "UIDevice.swift" -exec sed -i '' 's/return TARGET_OS_SIMULATOR != 0/return false/g' {} +
          echo "✅ Patch appliqué sur UIDevice.swift"

      # Prebuild génère les dossiers natifs (/ios)
      - name: ⚙️ Expo Prebuild
        run: npx expo prebuild --platform ios --clean --no-install

      # --- MURPHY FIX #1 : PATCH PODFILE ROBUSTE (RUBY) ---
      - name: 🔧 Patch Podfile for iOS Version (Ruby Script)
        run: |
          cd ios
          # Création d'un script Ruby temporaire pour modifier le Podfile proprement
          # Cela évite les erreurs de 'sed' si le format du fichier change légèrement
          cat <<EOF > patch_podfile.rb
          file_path = 'Podfile'
          text = File.read(file_path)
          
          # Configuration à injecter
          config_block = <<~RUBY
            installer.pods_project.targets.each do |target|
              target.build_configurations.each do |config|
                config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.4'
                config.build_settings['GCC_WARN_INHIBIT_ALL_WARNINGS'] = "YES"
                config.build_settings['SWIFT_SUPPRESS_WARNINGS'] = "YES"
              end
            end
          RUBY

          # On insère la config juste après le début du bloc post_install existant
          if text.include?('post_install do |installer|')
            new_text = text.sub('post_install do |installer|', "post_install do |installer|\n#{config_block}")
            File.write(file_path, new_text)
            puts "✅ Podfile patché avec succès (Injection)."
          else
            # Si pas de bloc, on en crée un à la fin
            File.open(file_path, 'a') do |f|
              f.puts ""
              f.puts "post_install do |installer|"
              f.puts config_block
              f.puts "end"
            end
            puts "✅ Podfile patché avec succès (Ajout fin)."
          end
          EOF
          
          # Exécution du script
          ruby patch_podfile.rb
          rm patch_podfile.rb
          
          echo "--- Vérification du Podfile (Début) ---"
          head -n 20 Podfile
          echo "--- Vérification du Podfile (Fin) ---"
          tail -n 20 Podfile

      - name: 🔧 Install Pods
        run: |
          cd ios
          pod install --repo-update

      - name: 🔨 Build Unsigned .app with Xcode
        run: |
          cd ios
          
          # --- CORRECTION CRITIQUE DU WORKSPACE ---
          WORKSPACE=$(find . -maxdepth 1 -name "*.xcworkspace" | head -n 1)
          if [ -z "$WORKSPACE" ]; then
             echo "⚠️ Pas de workspace racine trouvé, recherche plus large..."
             WORKSPACE=$(find . -name "*.xcworkspace" -not -path "*.xcodeproj/*" | head -n 1)
          fi
          echo "✅ Workspace Correct trouvé : $WORKSPACE"
          
          # DÉTECTION INTELLIGENTE DU SCHEME
          if xcodebuild -workspace "$WORKSPACE" -list | grep -q "Praxis"; then
            SCHEME="Praxis"
          elif xcodebuild -workspace "$WORKSPACE" -list | grep -q "GTAK"; then
            SCHEME="GTAK"
          else
            SCHEME=$(xcodebuild -workspace "$WORKSPACE" -list | sed -n '/Schemes:/,$p' | tail -n +2 | head -n 1 | xargs)
          fi
          
          if [ -z "$SCHEME" ]; then
             echo "❌ ERREUR : Aucun Scheme trouvé. Forçage à 'Praxis'."
             SCHEME="Praxis"
          fi
          echo "✅ SCHEME SÉLECTIONNÉ : $SCHEME"
          
          # Utilisation d'un chemin ABSOLU pour DerivedData
          DERIVED_DATA_PATH="$(pwd)/build_xc"
          mkdir -p "$DERIVED_DATA_PATH"

          # Compilation pour ARM64 (Device) sans signature
          xcodebuild \
            -workspace "$WORKSPACE" \
            -scheme "$SCHEME" \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -derivedDataPath "$DERIVED_DATA_PATH" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_IDENTITY="" \
            DEVELOPMENT_TEAM="" \
            DISABLE_MANUAL_TARGET_ORDER_BUILD_WARNING=YES \
            COMPILER_INDEX_STORE_ENABLE=NO \
            CLANG_ALLOW_NON_MODULAR_INCLUDES_IN_FRAMEWORK_MODULES=YES \
            GCC_TREAT_WARNINGS_AS_ERRORS=NO \
            SWIFT_TREAT_WARNINGS_AS_ERRORS=NO \
            ONLY_ACTIVE_ARCH=NO

      - name: 📦 Package into .ipa (Payload)
        run: |
          cd ios
          DERIVED_DATA_PATH="$(pwd)/build_xc"
          
          mkdir Payload
          # --- MURPHY FIX #4 : Trouver le bon .app ---
          # On cherche spécifiquement l'app qui ne contient pas 'Tests' et qui est dans le dossier Release
          APP_PATH=$(find "$DERIVED_DATA_PATH/Build/Products/Release-iphoneos" -name "*.app" -maxdepth 1 | grep -v "Tests" | head -n 1)
          
          if [ -z "$APP_PATH" ]; then
            echo "❌ ERREUR : Aucun fichier .app trouvé dans $DERIVED_DATA_PATH"
            ls -R "$DERIVED_DATA_PATH/Build/Products/Release-iphoneos"
            exit 1
          fi
          
          echo "✅ Packaging de l'application : $APP_PATH"
          cp -r "$APP_PATH" Payload/
          
          # On zippe le dossier Payload pour créer l'IPA
          zip -r GTAK-unsigned.ipa Payload

      - name: 📤 Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ios-v${{ github.run_number }}
          name: "Praxis iOS Unsigned #${{ github.run_number }}"
          files: ios/GTAK-unsigned.ipa
          draft: false
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
